'use strict'

module.exports = {
  app: require('./app'),
  page: require('./page'),
  rendered: require('./rendered'),
  router: require('./router'),
  display: require('./display'),
  control: require('./control')
}
